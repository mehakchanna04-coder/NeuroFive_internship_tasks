version: "3.9"

services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: job_board
    ports:
      - "5432:5432"
    volumes:
      - job_board_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build: .
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/job_board
      JWT_SECRET: dev-secret-change-me
      JWT_EXPIRES_IN: 7d
      PORT: 4000
      NODE_ENV: production
    ports:
      - "4000:4000"

volumes:
  job_board_pgdata:
